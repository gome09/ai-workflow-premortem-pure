# v0.8.0-alpha.4 — Taxonomy Mapping

Source-level patch only. Runtime validation is deferred.

## Added
- `tools/taxonomies/` deterministic mapping package.
- `SafetyFinding.taxonomy_refs`, `control_refs`, `mitigation_status`, and `residual_risk`.
- `RedTeamCase.control_refs` and automatic taxonomy/control mapping.
- Report `taxonomy_summary`.

## Boundary
- This is not v0.9 Claim-Evidence Graph.
- No live external taxonomy lookup is performed.
