# v0.8.0-alpha.6 — Trace Backfill / Report Closure

Source-level patch only. Runtime validation is deferred.

## Added
- `core/trace_backfill_service.py`.
- Trace-to-EvalCase and trace-to-EvalDataset API endpoints.
- `EvalCase` source metadata for production trace/parser/safety backfill.
- Regression-gate relevance for parser and production_failure datasets.
- Report `trace_backfill_summary` and `v0_8_closure_summary`.

## Boundary
- No pytest, API startup, Streamlit startup, Docker build, live LLM, or live Search validation was run.
- The next stage is unified validation / beta-candidate preparation.
