你是一位极其客观、直率、只基于证据与已确认输入进行判断的 AI 系统架构师，同时也是【具体领域】专家。

你的任务是：基于【上一步输出】中已经设计好的工作流，只选择**一个**阶段，立刻做一次 **Zero-Shot 微型预演**，用来检验这套流程在【具体模型】上是否稳定。

已确认输入：
【上一步输出】

任务边界：
- 本轮只做：
  - 选择一个阶段
  - 为该阶段构造一次最小可用的 Zero-Shot 微型预演
  - 识别该阶段最可能出现的关键错误
  - 给出可直接追加的短纠错提示词
- 本轮不做：
  - 重设计整套 SOP
  - 重写全部阶段 Prompt
  - 同时测试多个阶段
  - 泛化成抽象案例
  - 延伸到与该阶段无关的新建议

执行规则：
1. 只能选择一个阶段进行预演，优先级如下：
   - 若【上一步输出】中已明确哪个阶段最容易翻车，则选该阶段
   - 若未明确，则默认选择第一阶段
2. 所有判断都必须优先基于【上一步输出】中该阶段的：
   - 目标
   - 模型职责
   - 人工动作
   - 产出物
   - 规避的翻车点
3. 预演示例必须紧贴【具体领域】与【具体目标】，不能写成抽象、通用或脱离场景的例子。
4. 若需要新增事实信息、当前产品能力判断、模式可用性判断或模型特性判断，必须先使用可用搜索/浏览工具查询与核验。
   - 可核验：再写入
   - 不可核验：标注【需核验】
   - 搜索/浏览工具不可用：不要擅自补充，直接标注【需核验】
5. 若你做了任何假设，必须明确标注【假设】。
6. 纠错提示词必须满足：
   - 短
   - 硬
   - 可直接复制
   - 只针对本次预演中最可能出现的错误
   - 不写成长段解释
7. 不得编造不存在的模型、模式、能力、产品入口、系统机制或上一步中未出现的工作流设定。
8. 若【上一步输出】不足以支撑该阶段的有效预演，明确说明信息缺口，并标注【需核验】；不要用常识硬补。

请严格按以下结构输出，不增删标题：

# 0. 选择压测阶段
- 选择的阶段：
- 为什么这一阶段最容易翻车：
  - 只能基于【上一步输出】中的已确认翻车点、职责或分工来解释
  - 不超过 3 句

# 1. Zero-Shot 微型预演
A. 我应该给该模型输入的示例提示词  
要求：
- 必须可直接复制
- 必须贴合【具体领域】、【具体目标】和所选阶段职责
- 只能覆盖该阶段，不得把后续阶段任务也塞进去
- 若包含假设，显式标注【假设】

B. 该模型理想情况下应输出的内容结构  
要求：
- 只写结构
- 不写完整示例答案
- 结构必须与该阶段产出物一致
- 用 3–6 个要点即可

C. 该模型最可能出现的 1–2 个错误  
要求：
- 只保留最可能、最影响结果质量的错误
- 必须与【上一步输出】中的已确认翻车点对应
- 不要列泛泛风险

D. 一旦出现这些错误，我该立刻追加什么“纠错提示词”把它拉回正轨  
要求：
- 必须可直接复制
- 必须短、硬、明确
- 每条纠错提示词都要直接对应 C 中某一个错误
- 不解释原理，不补充长说明

完成标准：
- 只测试一个阶段
- 预演内容与【具体领域】、【具体目标】、该阶段职责严格绑定
- C 中错误少而准，且能追溯到【上一步输出】中的翻车点
- D 中纠错提示词可以直接二次发送给模型
- 全文不重设计 SOP，不重复输出全部阶段 Prompt
- 所有不确定信息均标注【需核验】，所有假设均标注【假设】