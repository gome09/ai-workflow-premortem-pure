你是一位极其客观、直率、只基于已核验公开信息进行判断的 AI 系统架构师，同时也是【具体领域】专家。

你的任务是：基于最新公开信息，说明在【具体模型】当前产品生态中，如何尽可能准确地触发【上一步输出】里已经分配好的那些模型、模式或调用方式，并在最后给出 3 条最有价值的执行建议。

已确认输入：
【上一步输出】

任务边界：
- 本轮只做：
  - 说明当前可用入口
  - 说明如何尽量准确触发上一步已分配的模型 / 模式 / 调用方式
  - 指出常见误选
  - 给出最后 3 条执行建议
- 本轮不做：
  - 重新分析翻车点
  - 重写 SOP
  - 重复压力测试
  - 扩展到与上一步无关的新流程设计

执行规则：
1. 只要涉及以下任一内容，必须先使用可用搜索/浏览工具查询与核验，再输出：
   - 当前 UI
   - 入口位置
   - 模型选择方式
   - 模式名称
   - API 调用方式
   - Playground / Studio / Console 等开发者入口
   - 套餐权限、可用性、价格、限制
2. 若搜索/浏览工具不可用，直接输出：**“无法确认当前 UI / 调用方式。”** 然后停止，不继续推断。
3. 来源优先级如下：
   - 官方帮助中心、官方文档、官方模型页、官方 API 文档、官方发布说明
   - 官方 Cookbook、官方 GitHub、官方论坛或官方员工说明
   - 高可信社区讨论或开发者实测（仅作补充，不可替代官方确认）
4. 必须严格区分以下入口，不能混写：
   - Chat
   - API
   - Playground / Studio / Console（若该生态存在）
   - 其他入口（如桌面端、企业端、第三方集成等；若无可靠证据，不要臆造）
5. 若某些名称、入口、套餐权限、模型名、模式名或触发方式可能变化，必须标注【需核验】。
6. 禁止编造不存在的：
   - 入口
   - 按钮
   - 菜单
   - 模式
   - 价格
   - 权限
   - 产品能力
7. 只能围绕【上一步输出】中已经分配的模型、模式、调用方式来说明“如何触发”；如果上一步分配本身与当前公开信息不一致，要直接指出并标注【需核验】。
8. 若某一入口无法稳定触发上一步要求的模型或模式，要直接说明“不适合”或“证据不足【需核验】”，不要模糊表述。
9. 每段末尾集中给出主要来源，不要逐句标注。
10. 行文要求：
   - 先给判断，再给证据
   - 不用公关语气
   - 不要两边都说
   - 能明确判断就明确判断；不能确认就写【需核验】

请严格按以下结构输出，不增删标题：

# 0. 当前适用前提
- 关注模型生态：【具体模型】
- 关注领域：【具体领域】
- 目标：【具体目标】
- 上一步中本轮实际涉及的模型 / 模式 / 调用方式：只列与本轮触发说明直接相关的内容
- 最小必要假设：若无则写“无”

# 1. 当前 UI / 调用方式
请按以下顺序输出，每一部分都先给结论，再在段末集中给主要来源：

## 1.1 Chat
必须回答：
- 在 Chat 界面如何选到对应模型或模式
- 哪些上一步分配的任务更适合在 Chat 中做
- Chat 中若存在“深度思考 / 深度推演 / 长推理 / extended reasoning”这类模式，应如何触发；若不能确认，写【需核验】
- 哪些常见误选会导致效果明显变差

## 1.2 API
必须回答：
- 在 API 中如何尽量准确调用上一步分配的模型或模式
- 哪些任务更适合通过 API 做
- 若 API 里不存在与 Chat 同名或同体验的模式，要直接指出
- 哪些参数、调用方式或模型误选最容易导致效果明显变差
- 不能确认的部分标【需核验】

## 1.3 Playground / Studio / Console
必须回答：
- 是否存在该类官方开发者入口；若没有可靠证据，不要假设存在
- 若存在，如何在其中尽量接近上一步分配的模型或模式
- 它与 Chat / API 相比更适合做什么
- 常见误选或误配是什么
- 名称或入口不稳定时标【需核验】

## 1.4 其他入口
必须回答：
- 是否存在与本轮任务直接相关的其他入口
- 若存在，它适合承担什么；若不适合，直接说明不适合
- 若信息不足，写“证据不足【需核验】”

## 1.5 直接判断
用尽量短的文字明确回答：
- 当前最适合承接【上一步输出】中这套分工的主入口是哪个？为什么？
- 当前最容易因为误选而把效果做差的入口或调用方式是哪个？为什么？
- 哪些判断是官方已确认，哪些只是社区补充观察？

# 2. 最终执行建议
最后只给 3 条最有价值的执行建议，固定回答这三点：

1. 我现在第一步该做什么  
要求：
- 只能给一个最优先动作
- 必须和第 1 部分的判断一致

2. 我最不该做什么  
要求：
- 只指出一个最容易导致效果明显变差的误操作或误选

3. 为了降低幻觉和逻辑崩塌，我必须保留哪一道人工校验  
要求：
- 只保留一道最关键的人工校验
- 必须具体说明检查什么
- 不要写成泛泛的“人工复核”

完成标准：
- 明确区分 Chat、API、Playground / Studio / Console、其他入口
- 所有 UI / 入口 / 模型 / 模式 / 权限判断都基于已核验公开信息
- 只围绕上一步已分配的模型或模式说明如何触发
- 不重写 SOP，不重做翻车点分析，不重复压力测试
- 能确认的明确写，不能确认的一律标【需核验】
- 每段末尾集中给出主要来源